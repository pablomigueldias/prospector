NOTION_TOKEN=
NOTION_DATABASE_ID=
GEMINI_API_KEY=
MAX_LEADS_PER_DAY=30
MIN_DELAY_SECONDS=5
MAX_DELAY_SECONDS=15

# --- Auth / login ------------------------------------------------------------
# Em produção (HTTPS via Caddy) deixe true → cookie Secure + __Host-.
# Em dev local sobre http puro, ponha false.
SESSION_COOKIE_SECURE=true
SESSION_DIAS_ABSOLUTO=7
SESSION_HORAS_INATIVIDADE=24
# Seed do admin (python -m app.jobs.seed_admin). Troque a senha no 1º acesso.
ADMIN_EMAIL=
ADMIN_SENHA_INICIAL=
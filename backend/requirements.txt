# Core - API e validação
fastapi==0.115.0
uvicorn[standard]==0.32.0
# Upload multipart (UploadFile/Form)
python-multipart==0.0.12
pydantic==2.9.0
pydantic-settings==2.6.0
python-dotenv==1.0.1

httpx[socks]==0.27.2
beautifulsoup4==4.12.3
playwright==1.48.0
lxml==5.3.0

# Integrações externas
notion-client==2.2.1
google-generativeai==0.8.3
# Storage S3-compatível (MinIO no VPS; portável pra S3 real)
boto3==1.35.49

# SQLAlchemy
sqlalchemy[asyncio]==2.0.36
# Driver async pro Postgres (mais rápido que psycopg pra muitos casos)
asyncpg==0.30.0
# Driver sync (usado pelo Alembic, que não fala async)
psycopg[binary]==3.2.3
# Versionamento de schema (gera migrations no Git)
alembic==1.13.3

# Auth — hashing de senha Argon2id (login)
argon2-cffi==23.1.0
# Auth — 2FA TOTP (pyotp) + QR do setup (qrcode/pillow). O secret TOTP é
# cifrado com Fernet (cryptography, já presente nas deps transitivas).
pyotp==2.9.0
qrcode[pil]==7.4.2

# Utilitários
tenacity==9.0.0
python-slugify==8.0.4
loguru==0.7.2